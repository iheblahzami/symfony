{% extends 'base.html.twig' %}

{% block title %}Savings{% endblock %}

{% block body %}
    <h1>Savings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saving.id }}</td>
            </tr>
            <tr>
                <th>MontantTotal</th>
                <td>{{ saving.montantTotal }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ saving.mois ? saving.mois|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_savings_index') }}">back to list</a>

    <a href="{{ path('app_savings_edit', {'id': saving.id}) }}">edit</a>

    {{ include('savings/_delete_form.html.twig') }}
{% endblock %}
